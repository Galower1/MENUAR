import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const Viro = () => {
  return (
    <View>
      <Text>Viro page</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
